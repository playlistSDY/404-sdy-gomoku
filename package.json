{
  "name": "sdy-gomoku",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": ".venv/bin/python -m uvicorn app.main:app --host 127.0.0.1 --port 5195",
    "start": "python -m uvicorn app.main:app --host 0.0.0.0 --port 5404",
    "verify:api": "python3 scripts/verify_api.py",
    "verify:render": "node scripts/verify-render.mjs"
  },
  "devDependencies": {
    "playwright-core": "^1.60.0"
  },
  "dependencies": {
    "three": "^0.184.0"
  }
}
